# P60 SessionRx — Decision Log

| ID | Date | Decision | Authority | Rationale |
|---|---|---|---|---|
| D-001 | 2026-09-20 | Build in `case-studies/project_60_session_rx/`, npm package name `session-rx` | ORCHESTRATOR | Ledger binds P60 to this path; `git init` already done here (§0.11 ONE_PROJECT_ONE_REPO). Creating a second dir `case-studies/session-rx/` would double-track. Package name in `package.json` is `session-rx` as the brief requires; only the on-disk folder differs. |
| D-002 | 2026-09-20 | All 8 CLI log formats resolved by direct filesystem probe, not web search | ORCHESTRATOR | INV-9 VERIFY_BEFORE_CLAIM. 6 CLIs are installed on this machine with real session data; formats read from actual files. Grok Build CLI and Amp are NOT installed → detection-only, no parser invented (brief: "do not invent formats"). |
| D-003 | 2026-09-20 | OpenCode collector reads ONLY `session`/`message`/`part`/`project`/`workspace`; NEVER `account`, `control_account`, `credential` | ORCHESTRATOR | INV-0.001 NO_SECRET_IN_THE_OPEN. `~/.local/share/opencode/opencode.db` `account` table holds `access_token` and `refresh_token` in plaintext. A session tool that reads them would surface secrets. Enforced by an allowlist in the collector plus a test. |
| D-004 | 2026-09-20 | Chart.js is vendored into `public/vendor/`, not loaded from CDN | ORCHESTRATOR | Brief quality gate: "Works offline — no CDN calls at runtime (bundle Chart.js)". This overrides the brief's earlier "Chart.js for graphs from CDN" line; the gate is the later and stricter statement. |
| D-005 | 2026-09-20 | Build all 16 waves in this one session rather than one wave per session (TB-10) | OPERATOR (explicit) | TB-10 exists so `factory_loop.sh` can measure per-wave cost. `factory_loop.sh` is NOT running for this session and no `.claude/factory_signal` wrapper is active (both verified). The operator's instruction was explicit: "be in autmode … keep working … provide me the final working repo". Splitting a 16-wave greenfield build across 16 sessions is not what was asked. INV-0.00001 (keep going until the work is done) governs. |
| D-006 | 2026-09-20 | Accept all 8 of codex's BLUEPRINT.md DIS-001..DIS-008 rulings unchanged | CODEX (design authority) | Most important: the `observed`/`not-observed`/`unknown` triad, never a zero substitute. A health tool that reports 0 sub-agents because it cannot read them is worse than one that says "unknown" — it converts "no evidence" into "all clear". Applies to Gemini repeats (DIS-003), sub-agents on 4 of 5 CLIs (DIS-004), Kimi absolute window (DIS-005), Gemini toolResultBytes (DIS-006). |
| D-007 | 2026-09-20 | All four external write-tier CLI workers are EXHAUSTED. Delegation continues via Claude Code sub-agents (the tier CLAUDE.md's Role Lock names for delegated writes). Orchestrator does NOT absorb the writing. | D-4 probe evidence below | See probe table. Codex wrote docs/BLUEPRINT.md and part of Phase 1 before its limit hit mid-wave. |

### D-007 probe evidence (D-4: an attempt with a recorded failure)

| Worker | Command actually run | Recorded failure output |
|---|---|---|
| codex | `codex exec --dangerously-bypass-approvals-and-sandbox` × 4 (blueprint + waves 1A/1B/1C) | `ERROR: You've hit your usage limit. Upgrade to Plus to continue using Codex, or try again at Oct 20th, 2026 7:59 AM.` |
| agy | `agy -p "<write probe>" --dangerously-skip-permissions --mode accept-edits` | `RESOURCE_EXHAUSTED (code 429): Individual quota reached. Please upgrade your subscription to increase your limits. Resets in 93h52m37s.` |
| kimi | `kimi -w . --yes -p "<write probe>"` | `Quota exceeded, please upgrade your plan or retry later` |
| gemini | `gemini -p "<write probe>" --yolo` | `When using Gemini API, you must specify the GEMINI_API_KEY environment variable.` — the key exists in the vault but ONLY under another project's scope; resolving it into P60 would breach INV-7 NO_CROSS_PROJECT_LEAK. Blocked on policy, not just on config. |

Consequence recorded per D-6: Phase 1 wave 1A (package.json, README.md, LICENSE) produced NOTHING
before codex died; waves 1B/1C produced files that must be treated as UNVERIFIED partial work and
re-reviewed, not trusted. Phases 2-5 are written by Claude Code sub-agents. The orchestrator writes
no production code, runs every gate itself, and owns all git operations (§0.19).
| D-008 | 2026-09-20 | Wave 1A VERIFIED by the orchestrator re-running its gate, not by trusting the worker | INV-9A | `npm pkg get` returns name session-rx + valid bin + files whitelist; `npm pack --dry-run` = 8 files / 10.3 kB with 0 hits for docs/ tests/ .factory .claude/ .env out/. The `repository` URL remains UNVERIFIED (F-004) and must be settled before publish. |
| D-009 | 2026-09-20 | Wave 2A's TWO deviations from my brief are APPROVED — my brief was wrong, the worker's data was right | ORCHESTRATOR, on independently re-probed evidence | (a) Codex `cacheCreate` read from `cache_write_input_tokens` rather than hardcoded `null`: my own re-probe of 1,384 `token_count` records across 60 rollouts found the field present in **all 2,692** usage buckets. Hardcoding `null` would have discarded a real, always-present measurement. (b) `custom_tool_call` parsed alongside `function_call`: my re-probe of 4 large rollouts found 193/509/67/67 `custom_tool_call` vs 0/16/0/1 `function_call` — **two of four sampled sessions have ZERO `function_call` records**, so a `function_call`-only parser would have reported 0 tool calls for real sessions. This is the difference between a working tool and a silently broken one. |
| D-010 | 2026-09-20 | Wave 2A VERIFIED: 32/32 tests pass on orchestrator re-run AND under an empty `HOME` | INV-9A | The empty-HOME run is the one that matters — it proves `npm test` passes on a fresh clone with no AI CLI installed, so contributors can run the suite. |
| D-011 | 2026-09-20 | F-001 (unreachable 1M window entry) escalated from MEDIUM to MUST-FIX-BEFORE-PHASE-3 | ORCHESTRATOR + wave 2A, independently | Wave 2A produced harder evidence than mine: real session `1ecace68` has peakContext=416,200 against window=200,000 → fraction 2.08. BP-003.01 warns above 0.70×window, so EVERY long Opus session would fire the context alarm regardless of actual pressure. Phase 3 wires that rule, so the table must be fixed first. |
| D-012 | 2026-09-20 | RATIFIED wave 1B-fix2's deviation: the known-window tier ladder is VENDOR-SCOPED first, global only as fallback | ORCHESTRATOR, verified | My ruling said "smallest known tier >= observedFloor". Implemented literally, that lent **gpt-5's 400,000 tier to an Anthropic session**: real session `82aef9a3` (claude-opus-5, peak 368,963) promoted to 400,000 → 0.922 of window → BP-003.01 false alarm. Vendor-scoping fixes it (that session now reads 0.369) and is still a MECHANISM, not a hardcode: a window size counts as known *for* a model only if that model's vendor is known to ship it. Verified: claude-opus-5 floors 368,963/416,200/999,767 → Anthropic 1M tier (0.369/0.416/1.000); gpt-5 at 350,000 stays on its own 400,000 table tier. |
| D-013 | 2026-09-20 | Real data is WORSE than my F-008 estimate and the fix does not over-correct | Wave 1B-fix2, verified | Largest live session reads **4.995× window**, not the 2.08× I measured. Fractions above 1.0: **20/20 before → 0/20 after** on the 20 largest real sessions. Crucially 7 sessions still warn at 0.96–0.999 of a 1M window — those are CORRECT alarms, and a test pins that the fix does not silence them. A fix that quietly suppressed real warnings would have been the worse failure. |
| D-014 | 2026-09-20 | Where window.source is `observed-floor`, the context fraction is `unknown` — no percentage, no threshold verdict | ORCHESTRATOR (F-014) | floor/floor = 1.0 by construction, so a naive reading warns on 100% of OpenCode sessions including trivial 5,000-token ones. The window may still be DISPLAYED as a lower bound ("at least 41,344") because that is true; turning a lower bound into a percentage is what is forbidden. `observed-promoted` is unaffected — its denominator is a real vendor tier. |
| D-015 | 2026-09-20 | Wave 2C VERIFIED — the OpenCode security locks hold against the REAL 10 GB database | INV-9A, orchestrator re-run | 132/132 suite. Only `account`/`credential` references in opencode.js are the DENY regex and its error message. Zero `SELECT *` in src/. Real DB: 3 sessions in 7 ms, **mtime and size byte-identical before and after** collection ⇒ provably read-only, no WAL write lock. The fixture DB is built WITH populated `account`/`control_account`/`credential` tables, so the leak test had four real planted values to catch — there was genuinely something to leak. |
| D-016 | 2026-09-20 | Wave 3C VERIFIED — report redaction holds against adversarial strings I constructed myself, not the worker's fixtures | INV-9A | 7/7: `authorization: Bearer <vendor-prefixed token>` redacted (2 passes), `Bearer <gh-prefixed>` redacted, keyed `access_token`/`refresh_token` redacted, bare 64-char hex run redacted — AND the two that must SURVIVE do: a 96-char filesystem path (0 redactions) and a session UUID (0 redactions). Paths are the report's primary evidence per FVA-002, so over-redaction would have gutted the feature. 165/165 suite. |
| D-017 | 2026-09-20 | Two real security bugs were found and closed by wave 3C during the build, not by review afterwards | Wave 3C, verified by D-016 | (a) `authorization: Bearer <tok>` would have LEAKED the token: the keyed-credential pattern's value alternation stops at the space after `Bearer`, redacting the word and leaving the secret. Closed twice over (order-independent). (b) A naive `[A-Za-z0-9+/]{40,}` base64 rule ate ordinary deep filesystem paths — the report's primary evidence reference. Unpadded runs now exclude `/`; padded base64 keeps it since no path carries a trailing `=`. My probe confirms both. |
| D-018 | 2026-09-20 | F-007/F-012/F-013 VERIFIED, and the git check WINT2 correctly declined to run | INV-9A + §0.19 | `npm test` EXIT=0 with 191/191 (was EXIT=1 with a module error while the suite was green). `npm run test:collectors` EXIT=0. `git check-ignore -v test_output.txt` → `.gitignore:30`, and it is untracked. WINT2 correctly refused to run `git check-ignore` itself and left it to me — §0.19 WORKER_GIT_BOUNDARY observed without being reminded. |
| D-019 | 2026-09-20 | ACCEPTED WINT2's disclosed beyond-brief fix: `collectAll` now folds in each collector's own diagnostic | ORCHESTRATOR, verified on real data | `claude`/`codex` published diagnostics on `this.lastDiagnostic` instead of the passed-in `diagnostic` option, so their diagnostics were unreachable downstream — the SAME defect class as F-009/F-013. That mattered because `windowPromotions` (F-008's entire visibility mechanism) rides on the diagnostic: **3 genuine window promotions on real Claude sessions were being silently dropped.** Fixing the number while hiding the evidence would have defeated item 2 of my own F-008 ruling. Revert cost is one line plus 2 of 18 tests. |
| D-020 | 2026-09-20 | F-008 + F-009 + F-014 VERIFIED END-TO-END ON REAL DATA — this is the gate that mattered | INV-9A, orchestrator probe | Across the real corpus via `collectAll`: **1,309 turns carry a resolvable context fraction and 0 exceed 1.0** (was 20/20 of the largest sessions above 1.0, worst 4.995x). Promotions arrive downstream with full provenance: `{model: claude-opus-5, tableSaid: 200000, promotedTo: 1000000, ladder: vendor, entry: claude-family}`. Real Claude sessions with peaks 415,223 / 256,047 / 416,200 now resolve to a 1,000,000 window as `observed-promoted`, i.e. fractions 0.415 / 0.256 / 0.416 — all below the 0.70 warn line, so the 69%-false-alarm class is eliminated rather than merely re-labelled. |
| D-021 | 2026-09-20 | Wave INT-1 VERIFIED, INCLUDING an INV-6 audit of its four changed test expectations | INV-6 + INV-9A | INV-6 forbids changing tests to make them pass, so I audited rather than accepting the explanation. VERDICT: legitimate contract change, not bent tests. (a) every test file is new/untracked this session, so nothing in git history was overwritten; (b) each changed assertion carries a comment citing F-008/F-014; (c) decisively, `tests/collectors/claude.test.js:157` STILL pins `{tokens: null, source: "unknown"}` for the no-reading-at-all case — the never-guess guarantee is intact, which is what INV-6 is protecting. resolveWindow adoption went 0/0/0/0/0 → present in all five collectors. |
| D-022 | 2026-09-20 | F-014 VERIFIED IN PRODUCTION DATA — max published context fraction 4.995 → 0.417 | INV-9A, orchestrator probe of PUBLISHED values (not my own division) | 264 real `observed-floor` turns publish a fraction: **0 of 264**. 3,769 `observed-promoted` turns publish one: all of them, correctly, since their denominator is a real vendor tier. Max published fraction across real sessions **0.417**. I first measured 1.000 by dividing manually, then re-probed the collector's PUBLISHED `context.fraction` — the distinction matters: the raw ratio is 1.0 by construction and the product correctly refuses to publish it. |
| D-023 | 2026-09-20 | ACCEPTED wave INT-1's third trap, which no brief of mine anticipated | ORCHESTRATOR | Taking the observed floor from MAIN-CHAIN turns only (the intuitive reading, since `model` is `mainModel ?? anyModel`) leaves a session with main chain 90,000 + sidechain 350,000 pinned at a 200,000 window, publishing the sidechain turn at **1.75**. Every turn divides by ONE session window, so the floor must span every turn including sidechains. Guarded by a test and mutation M2. Also accepted: `contextFraction` added as a shared export in collectors/base.js rather than copied into three collectors — three private copies is precisely how a critical ruling drifts unnoticed in one of them. |
| D-024 | 2026-09-20 | Wave 3B VERIFIED — gaps are null, never zero, confirmed on real data | INV-9A, orchestrator probe | `avgContextPerTurn`: null=7, **zero=0**, pos=8. `cacheHitRate`: null=8, **zero=0**, pos=7. `highContextPct`: null=10, zero=5 — the 5 zeros are days that genuinely measured 0% above threshold, so the series DISTINGUISHES "measured zero" from "could not measure", which is the distinction the product rests on. Heatmap 15x24, 58 non-zero cells, local-time bucketed (offset -330). 290 turns excluded from G1 for having no computable fraction (observed-floor 288, unknown 2) and the exclusion is REPORTED, so the denominator is honest. |
| D-025 | 2026-09-20 | ACCEPTED wave 3B's self-correction: `stable` now requires the PRIMARY metric to be decidable | ORCHESTRATOR | Its first version reported `stable` on real data while "% of turns above 70% of window" — the metric BP-003.01 actually alarms on — had only 5 measured days and was never decidable. That is a clean bill of health issued off half the evidence: the F-014 failure mode wearing a different hat. The worker caught it on its own real-data gate, not in review. The shipped verdict is now `unknown` with a reason naming both causes. A measured DECLINE in the secondary metric is still reported, because a reported decline can never be a false all-clear — only a false "fine" can. |
| D-026 | 2026-09-20 | F-008's payoff, quantified on a real day | Orchestrator probe | 2026-09-16: 360 turns, avg 237,666 tokens/turn, `highContextPct` 0 — correctly 24% of a promoted 1,000,000 window. Under the old 200,000 table that same day reads 119% and fires the context alarm. One day, one number, the whole reason F-008 mattered. |
| D-027 | 2026-09-20 | Wave 3A VERIFIED — 157 of 240 real verdicts are `unknown`, each with a reason | INV-9A | My brief said an output with no `unknown` anywhere means the honesty contract is not wired and I would reject the wave. It is comprehensively wired: all 6 `observed-floor` OpenCode sessions return R1 `unknown` (not the 1.00 warn); all 6 `observed-promoted` Claude sessions return R1 `pass` against the promoted 1M window; Gemini's and Kimi's 96 verdicts are all unknown and none renders as a pass. 334/334 suite, 72 analyzer tests. Mutations M3 (the F-014 guard) and M4 (`unknown` counted as passed) target the honesty contract itself — both caught. |
| D-028 | 2026-09-20 | ACCEPTED wave 3A's own ruling: zero tool calls requires corpus evidence the CLI surfaces tool calls at all | ORCHESTRATOR | Its first real run showed Kimi `pass` on R3/R4 with zero tool calls recorded — a false all-clear built from a collector gap, not from clean behaviour. Zero-tool-calls now yields `unknown` unless `ctx.toolCallsRecorded` proves the collector surfaces them. Stated cost: the verdict depends on how much was collected. Correct trade — an honest unknown beats a fabricated clean. |
| D-029 | 2026-09-20 | Wave 4A VERIFIED by review of its stated mechanisms; symlink refusal ACCEPTED | ORCHESTRATOR | preview cannot lie because preview/apply/undo/check all run through ONE `computeTargets()` — no second code path can emit different bytes. CLAUDE.md is never decoded/re-encoded (`Buffer.concat([beforeBytes, section])`, with a test asserting prior bytes are a literal PREFIX of the result). Undo hash-verified at three points across CRLF, no-trailing-newline, non-ASCII. Display-only fixes extend `FixBase` not `WritableFix`, so `apply` is absent from the whole prototype chain and frozen prototypes throw on assignment — structurally impossible to write. ACCEPTED: a symlinked target is refused, because `rename` onto a symlink replaces the link with a regular file and a developer symlinking `~/.claude/CLAUDE.md` into a dotfiles repo would silently lose it. |
| D-030 | 2026-09-20 | RULING: close rule 6 (sub-agents) rather than ship a rule that never fires | ORCHESTRATOR (F-016) | 87 subagents dirs / 1,093 `agent-*.jsonl` confirmed, `isSidechain` true inside them and true=0 / false=138,358 in main transcripts. The operator specified six rules; five can produce a verdict, the sixth cannot on any CLI. Data is rich and two CLIs can support it — Claude via these files, Kimi via `SubagentEvent` (36% of its records). Completing specified scope, not expanding it. |
| D-031 | 2026-09-20 | THE DECISIVE SAFETY CHECK: no fix test ever touched a real config file | INV-9A, orchestrator probe | `~/.claude/CLAUDE.md` mtime 08:30 — BEFORE this session's build began — size 28,844, and **0 `session-rx:` markers**. `~/.claude/settings.json` has **0 `autoCompact`**. `~/.session-rx/` **does not exist**, so no real `apply()` has ever run. Two waves wrote 5 fixes and 90 tests against the real file formats without ever writing a real file. This was the highest-risk property in the project. |
| D-032 | 2026-09-20 | APPROVED wave 4B's deviation: BP-004.02's literal body was replaced with concrete, actionable text | ORCHESTRATOR | BP-004.02's literal body is "Keep tool output concise; request bounded, relevant output and summarize large results before continuing." That is precisely the vague advice my brief forbade — it changes no agent's behaviour and would embarrass the project in a README. The shipped text names the actual mechanisms (`\| head -50`, `git diff --stat` before `git diff`, redirect-then-cite, grep-then-read-a-window) and states the measured trigger: three or more tool results over 10 KiB. Marker, target and heading match BP-004.02 exactly, so idempotency and the fix contract are untouched. When the blueprint's letter and the operator's quality bar conflict, the quality bar wins and the deviation gets disclosed — which it was. |
| D-033 | 2026-09-20 | Wave 4A's REPORT contained one unverified claim; its CODE is unaffected | INV-5, caught by wave 4B, confirmed by me | R_4A §7 stated that `tests/fixes-base.test.js` asserts BP-004.02's body verbatim. It does not: `grep -n "output-hygiene\|BP-004.02\|Keep tool output concise" tests/fixes-base.test.js` returns NOTHING. Wave 4B checked the claim before relying on it rather than trusting a peer's report, which is exactly right. Recorded because a worker report is evidence and a false line in one can mislead a later wave — no code defect, and 4A's verified mechanisms (D-029) stand. |
| D-034 | 2026-09-20 | Waves 4B + 4C VERIFIED: 5 fixes, 90 tests, suite 421/421 exit 0 | INV-9A | All five modules carry a stable marker and a ruleId binding them to the rule they answer. 4C proved the case real users hit: all three of its sections applied to the SAME CLAUDE.md coexist and undoing the middle one leaves the other two intact — where naive marker handling breaks. `src/fixes/base.js` sha256 unchanged by both waves (`bc1d8a74…e96a`), so neither forked the write path. |
| D-035 | 2026-09-20 | Wave 5A VERIFIED END-TO-END: the tool actually runs and diagnoses real sessions | INV-9A, orchestrator ran the real binary | `node src/cli.js` → "SessionRx is serving http://127.0.0.1:7331/ — Ready in **6 ms** — local only, no network requests" (budget was 3,000 ms). `/api/collectors` returns the 5 real detected CLIs with their real paths. `/api/sessions?scan=25` returns **125 real sessions**. Suite 485/485. Worker additionally installed the packed tarball in a temp project and ran `npx session-rx` successfully. |
| D-036 | 2026-09-20 | F-019 CLOSED, and closed better than I specified — redaction FAILS CLOSED | INV-0.001, orchestrator read the code path | I promised to verify that 4A's cross-wave warning was honoured. `sendJson` is the SINGLE JSON path for every route including `/api/fixes/*`, and if the redactor module cannot load it returns **503 with a fixed message rather than unredacted data**: "Without the redactor we cannot prove the body is credential-free". I had asked for redaction; the worker made it fail closed. That is the difference between a security control and a security intention. |
| D-037 | 2026-09-20 | ACCEPTED 5A's bounded corpus scan with explicit `atLimit` honesty | ORCHESTRATOR | Measured on the real corpus: 50 sessions/collector = 0.79 s, 250 = 3.4 s, **unbounded had not finished after 60 s**. Default is the 250 newest per collector, and every response carries `scan:{limitPerCollector, defaulted, atLimit, note}`. `atLimit:true` states plainly that older unread sessions exist, and a miss on `/api/sessions/:id` says "not found in the scanned window" rather than "does not exist". Bounding is required for a 3-second start; saying so in the payload is what keeps it honest. |
| D-038 | 2026-09-20 | RULING on `package-lock.json`: keep it and TRACK it | ORCHESTRATOR | 5A found `node_modules/` did not exist — `express` was declared but never installed, so nothing could run — and ran `npm install`, which created a lockfile at the root outside its owned paths. `package.json` is byte-identical (md5 unchanged), so only declared deps were installed. session-rx is a CLI application run via `npx`, not a library, so a committed lockfile is the correct, reproducible choice. It must NOT be gitignored. |
| D-039 | 2026-09-20 | BP-005.04 `charts.tools` does not exist — the BLUEPRINT row is wrong, not the code | ORCHESTRATOR | Wave 3B's `buildTrends` returns `{context, spend, cache}`: it measures cache spend, not tool counts. 5A passed 3B's real shape through verbatim rather than emitting `tools: []`, which would have rendered in the UI as "no tool problems" — a fabricated all-clear. Correct call. Same class: `heatmap` is a labelled object (`rows:15, cols:24, orientation:"day-major"`), not a bare array. BP-005.04 needs correcting to match shipped reality (INV-2 drift, tracked). |
| D-040 | 2026-09-20 | Wave 5C VERIFIED — Chart.js 4.5.1 is the real library, with reproducible provenance | INV-9A, orchestrator re-hashed the file | `public/vendor/chart.umd.min.js` is 208,522 B and its sha256 is `48444a82d4edcb…c9f54a`, matching the worker's claim EXACTLY; the v4.5.1 banner is present; LICENSE and PROVENANCE.md ship alongside. The worker verified the tarball's sha1 and sha512 against the registry's own `dist.shasum` / `dist.integrity` **before extraction** — real supply-chain verification, not a download-and-hope. Stated gap it chose deliberately: the 967 kB source map is not vendored (one LOCAL 404 with devtools open), because stripping `sourceMappingURL` would have broken the reproducible hash. Correct trade. |
| D-041 | 2026-09-20 | `unknown` is distinguished by FOUR channels, not colour alone | ORCHESTRATOR, accepted | Hue deliberately off the green/amber/red ramp, plus a 135° hatch no other state uses, plus dashed borders where others are solid, plus italic type with a `?` glyph. This matters beyond aesthetics: colour alone would fail for a colour-blind user, and on real data 59% of verdicts are `unknown` — the state a user MUST not misread as a pass. The same separation is applied to measured-zero vs uncollected-hour in the heatmap. |
| D-042 | 2026-09-20 | ACCEPTED 5C's honest mutation report: 9/11 on the first run, 11/11 after | ORCHESTRATOR, INV-5 | It reported the 9/11 first because "the first number is the honest one". The two survivors were REAL test gaps, not noise: no test covered the `renderChart(canvas, callerConfig)` path `trends.js` actually uses, and the null-metric test asserted "some node" rather than the specific field. A worker that reports only its final green number hides exactly this. innerHTML audit: zero occurrences in code, with the comment-stripper itself under test. |
| D-043 | 2026-09-20 | Wave WSUB VERIFIED — rule 6 fires for the first time; sub-agent tokens provably never reach a parent | INV-9A | BP-003.06 went from `unknown` on 100% of sessions across all five CLIs to **0 unknown of 8,077 analyzed**, with 60 real `observed` findings (32 Claude, 28 Kimi). This session reads `dispatched=19, peakConcurrent=3`. No-inflation proof: the worker FROZE the corpus first — a live transcript grew 495→500→503 turns between passes, so "a live comparison proves nothing" — then showed all 97 parents deep-JSON identical on vs off, `sum(inputTokens)` 20,236,258,896 both ways. **55,661 sub-agent turns exist and not one token reaches a parent.** F-005 (U+2028) verified fixed by my own probe: 1 record, 0 skipped. It also corrected my brief's stale 485/485 baseline to the real 515/510/5-fail it measured. |
| D-044 | 2026-09-20 | Wave WFIX VERIFIED — F-023 closed without costing rule 6 anything | INV-9A, orchestrator probe | Headline sessions 156 → **50**; **110 sub-agent sessions set aside and COUNTED** (0 orphans, per-CLI breakdown) rather than silently dropped. Rule 6 on the frozen corpus is byte-identical before and after: 32 observed / 1,171 not-observed / 0 unknown. The safe design was PARTITIONING, not filtering: every session is still analyzed against all six rules and `childrenByParent` is built over the full set BEFORE the partition, so rule 6's evidence was never at risk. Proof went beyond my ask — **1,203 of 1,203** session-health objects deep-JSON identical, honesty contract re-measured over 1,362 sessions with 0 violations. Mutation M1 (emptying `ctx.children`) turned rule 6 100% unknown and all 32 findings into a false measured zero — the exact regression that mattered, and a test catches it. |
| D-045 | 2026-09-20 | Wave 5D VERIFIED — the UI renders; screenshot is real; and it refused to delete tests to make them pass | INV-6 + INV-9A | Suite 559/559, four page modules wired into index.html, console clean across all four tabs (0 messages, 0 page errors, 0 HTTP ≥400). Live score headline `3/6 checks passed · 2 problems observed · 1 could not be measured`; passed bar segment measured 66.6667% for 4/6 by `getComputedStyle`, so unknowns did not widen it. It corrected my ruling's factual error — `renderHeatmap` lived in `chart.js`, not `health-card.js` — and, crucially, did NOT delete the 11 assertions pointing at the retired module: the shipping pages had no behavioural test, so deleting them would have retired the TESTED card and left the UNTESTED one shipping. It retargeted all 11 at the real code. Retired files went to `_trash/` per INV-0, never `rm`. |
